# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: Sign yourself >> Verify that text widget Copy widget next to current widget function correctly in Sign Yourself.
- Location: /home/runner/work/OpenSign-Tests/OpenSign-Tests/tests/MainAppPages/SignYourSelf.spec.js:1420:1

# Error details

```
Error: Timed out 5000ms waiting for expect(locator).toHaveValue(expected)

Locator: locator('//textarea[@readonly and (not(@placeholder) or @placeholder!=\'text\')]')
Expected string: "20 wood street sanfransisco"
Received: <element(s) not found>
Call log:
  - expect.toHaveValue with timeout 5000ms
  - waiting for locator('//textarea[@readonly and (not(@placeholder) or @placeholder!=\'text\')]')

    at /home/runner/work/OpenSign-Tests/OpenSign-Tests/tests/MainAppPages/SignYourSelf.spec.js:1457:101
```

# Page snapshot

```yaml
- banner:
  - text: "New feature: Protect your account with Two-Factor Authentication (2FA) and enjoy the future of login with Passkeys — no passwords needed. — Setup now."
  - img
  - text: 
  - button ""
  - img "logo"
  - text: TEAM
  - button "Open app drawer": 
  - img "img"
  - button "Pravin Testing account"
  - button ""
- complementary:
  - img "Profile"
  - paragraph: Pravin Testing account
  - paragraph: OpenSign pvt ltd
  - navigation "OpenSign Sidebar Navigation":
    - menubar "OpenSign Sidebar Navigation":
      - menuitem "Dashboard"
      - menuitem "Sign yourself"
      - menuitem "Request signatures"
      - button " Templates"
      - menuitem "OpenSign™ Drive"
      - button " Documents"
      - menuitem "Contactbook"
      - button " Settings"
  - link "OpenSign's Github"
  - link "OpenSign's LinkedIn"
  - link "OpenSign's Twitter"
  - link "OpenSign's Discord"
- main:
  - text: Pages
  - button "+ Add pages"
  - text: +      
  - button [disabled]
  - text: 1 of 3
  - button
  - button "Back"
  - button "Finish"
  - img "signature"
  - text:   
  - textbox "text": 20 wood street sanfransisco
  - text: 20 wood street sanfransisco Widgets
  - superscript: "?"
  - list "Add widgets": " signature   stamp   initials   number #  name   job title   company   email   date   text   cells   checkbox   image "
  - complementary:
    - paragraph: "All rights reserved © 2026 OpenSign™ ( version: v2.41.0 )"
```

# Test source

```ts
  1357 |   await page.locator('//i[contains(@class, "fa-copy") and contains(@class, "icon")]').dblclick();
  1358 |   
  1359 |   const isVisible = await page.locator('//h3[text()="Copy widget to"]').isVisible();
  1360 |   
  1361 |   if (isVisible) {
  1362 |       console.log('"Copy widget to" is visible. Stopping the loop.');
  1363 |       break; // Exit loop once the element is visible
  1364 |   }
  1365 |
  1366 |   await page.waitForTimeout(500); // Small delay to prevent rapid clicking
  1367 | }
  1368 | await page.getByRole('radio', { name: 'All pages but last' }).check();
  1369 | await page.getByRole('button', { name: 'Apply' }).click();
  1370 | await expect(page.locator("//textarea[text()='20 wood street sanfransisco']")).toBeVisible();
  1371 |    await expect(page.locator('//div[@data-page-number="2"]//span[@readonly and normalize-space(.)="20 wood street sanfransisco"]')).toBeVisible();
  1372 | await expect(
  1373 |   page.locator('//div[@data-page-number="3"]//span[normalize-space(.)="20 wood street sanfransisco"]')
  1374 | ).toHaveCount(0);
  1375 | await commonSteps.clickFinishButtonOnPlaceholder();
  1376 | await page.getByText('Successfully signed!').waitFor({ timeout: 120000 });
  1377 | });
  1378 | test('Verify that text widget Copy widget to all pages but first function correctly in Sign Yourself.', async ({ page }) => {
  1379 |   const commonSteps = new CommonSteps(page);
  1380 |   // Step 1: Navigate to Base URL and log in
  1381 |   await commonSteps.navigateToBaseUrl();
  1382 |   await commonSteps.login();
  1383 | //const title = await page.title()
  1384 |   //Expects page to have a heading with the name of dashboard.
  1385 | //expect(title).toBe('Dashboard - OpenSign™');
  1386 |
  1387 | await page.getByRole('menuitem', { name: 'Sign yourself' }).click();
  1388 |   await page.locator('input[name="Name"]').fill('Offer Letter for QA1144');
  1389 |   await page.locator('input[name="Note"]').click();
  1390 |   const fileChooserPromise = page.waitForEvent('filechooser');
  1391 | await page.locator('input[type="file"]').click();
  1392 | const fileChooser = await fileChooserPromise;
  1393 | await fileChooser.setFiles(path.join(__dirname, '../TestData/Samplepdfs/Sample_Test_doc_line.pdf'));
  1394 | await expect(page.getByRole('button', { name: 'Next' })).toBeEnabled({ timeout: 90000 }); // Wait up to 90s
  1395 | await page.getByRole('button', { name: 'Next' }).click();
  1396 | await commonSteps.waitAndPlaceSignatureAndClickSave('signature', 600, 200);
  1397 | await commonSteps.dragAndDrop('text', 600, 300);
  1398 | await commonSteps.fillTextField('text', '20 wood street sanfransisco');
  1399 | await commonSteps.ClickSavebuttonSignerModal();
  1400 |   while (true) {
  1401 |     await page.locator('//i[contains(@class, "fa-copy") and contains(@class, "icon")]').dblclick();
  1402 |     
  1403 |     const isVisible = await page.locator('//h3[text()="Copy widget to"]').isVisible();
  1404 |     
  1405 |     if (isVisible) {
  1406 |         console.log('"Copy widget to" is visible. Stopping the loop.');
  1407 |         break; // Exit loop once the element is visible
  1408 |     }
  1409 |   
  1410 |     await page.waitForTimeout(500); // Small delay to prevent rapid clicking
  1411 |   }
  1412 |   await page.getByText('All pages but first').click();
  1413 | await page.getByRole('button', { name: 'Apply' }).click();
  1414 |  await expect(page.locator("//textarea[text()='20 wood street sanfransisco']")).toBeVisible();
  1415 |   await expect(page.locator('//div[@data-page-number="2"]//span[@readonly and normalize-space(.)="20 wood street sanfransisco"]')).toBeVisible();
  1416 | await expect(page.locator('//div[@data-page-number="3"]//span[@readonly and normalize-space(.)="20 wood street sanfransisco"]')).toBeVisible();
  1417 | await commonSteps.clickFinishButtonOnPlaceholder();
  1418 | await page.getByText('Successfully signed!').waitFor({ timeout: 120000 });
  1419 | });
  1420 | test('Verify that text widget Copy widget next to current widget function correctly in Sign Yourself.', async ({ page }) => {
  1421 |   const commonSteps = new CommonSteps(page);
  1422 |   // Step 1: Navigate to Base URL and log in
  1423 |   await commonSteps.navigateToBaseUrl();
  1424 |   await commonSteps.login();
  1425 | //const title = await page.title()
  1426 |   //Expects page to have a heading with the name of dashboard.
  1427 | //expect(title).toBe('Dashboard - OpenSign™');
  1428 |
  1429 | await page.getByRole('menuitem', { name: 'Sign yourself' }).click();
  1430 |   await page.locator('input[name="Name"]').fill('Offer Letter for QA1144');
  1431 |   await page.locator('input[name="Note"]').click();
  1432 |   const fileChooserPromise = page.waitForEvent('filechooser');
  1433 | await page.locator('input[type="file"]').click();
  1434 | const fileChooser = await fileChooserPromise;
  1435 | await fileChooser.setFiles(path.join(__dirname, '../TestData/Samplepdfs/Sample_Test_doc_line.pdf'));
  1436 | await expect(page.getByRole('button', { name: 'Next' })).toBeEnabled({ timeout: 90000 }); // Wait up to 90s
  1437 | await page.getByRole('button', { name: 'Next' }).click();
  1438 | await commonSteps.waitAndPlaceSignatureAndClickSave('signature', 600, 200);
  1439 | await commonSteps.dragAndDrop('text', 600, 300);
  1440 | await commonSteps.fillTextField('text', '20 wood street sanfransisco');
  1441 |   await commonSteps.ClickSavebuttonSignerModal();
  1442 | while (true) {
  1443 |   await page.locator('//i[contains(@class, "fa-copy") and contains(@class, "icon")]').dblclick();
  1444 |   
  1445 |   const isVisible = await page.locator('//h3[text()="Copy widget to"]').isVisible();
  1446 |   
  1447 |   if (isVisible) {
  1448 |       console.log('"Copy widget to" is visible. Stopping the loop.');
  1449 |       break; // Exit loop once the element is visible
  1450 |   }
  1451 |
  1452 |   await page.waitForTimeout(500); // Small delay to prevent rapid clicking
  1453 | }
  1454 | await page.getByText('Next to current widget').click();
  1455 | await page.getByRole('button', { name: 'Apply' }).click();
  1456 | await expect(page.locator("//textarea[@readonly and @placeholder='text']")).toBeVisible();
> 1457 | await expect(page.locator("//textarea[@readonly and (not(@placeholder) or @placeholder!='text')]")).toHaveValue("20 wood street sanfransisco");
       |                                                                                                     ^ Error: Timed out 5000ms waiting for expect(locator).toHaveValue(expected)
  1458 | await commonSteps.clickFinishButtonOnPlaceholder();
  1459 | await page.getByText('Successfully signed!').waitFor({ timeout: 120000 });
  1460 | });
  1461 | test('Verify that the merge page functions correctly and the user can sign the merged document in Sign Yourself.', async ({ page }) => {
  1462 |   const commonSteps = new CommonSteps(page);
  1463 |   // Step 1: Navigate to Base URL and log in
  1464 |   await commonSteps.navigateToBaseUrl();
  1465 |   await commonSteps.login();
  1466 | //const title = await page.title()
  1467 |   //Expects page to have a heading with the name of dashboard.
  1468 | //expect(title).toBe('Dashboard - OpenSign™');
  1469 |
  1470 | await page.getByRole('menuitem', { name: 'Sign yourself' }).click();
  1471 |   await page.locator('input[name="Name"]').click();
  1472 |   await page.locator('input[name="Name"]').fill('Offer Letter for QA1144');
  1473 |   await page.locator('input[name="Name"]').press('Tab');
  1474 |   await page.locator('input[name="Note"]').click();
  1475 |   const fileChooserPromise = page.waitForEvent('filechooser');
  1476 | await page.locator('input[type="file"]').click();
  1477 | const fileChooser = await fileChooserPromise;
  1478 | await fileChooser.setFiles(path.join(__dirname, '../TestData/Samplepdfs/Sample-Joining-Letter.pdf'));
  1479 | await expect(page.getByRole('button', { name: 'Next' })).toBeEnabled({ timeout: 90000 }); // Wait up to 90s
  1480 | await page.getByRole('button', { name: 'Next' }).click();
  1481 | await page.waitForSelector('//div[@class=\'react-pdf__Document\']', { timeout: 90000 }); 
  1482 | await page.waitForLoadState("networkidle");
  1483 | await page.locator('//span[normalize-space()="signature"]').waitFor({ state: 'visible', timeout: 90000 });
  1484 | await page.waitForLoadState("networkidle");
  1485 | await expect(page.locator('#renderList')).toContainText('1 of 1');
  1486 |   const fileChooserPromise2 = page.waitForEvent('filechooser');
  1487 |   await page.getByTitle('Add pages').nth(1).click();
  1488 |   const fileChooser2 = await fileChooserPromise2;
  1489 |   await fileChooser2.setFiles(path.join(__dirname, '../TestData/Samplepdfs/Sample_Test_doc_line.pdf'));
  1490 |   await expect(page.locator('#renderList')).toContainText('1 of 4');
  1491 | await page.locator(
  1492 |   "//div[contains(@class,'react-pdf__Document')]//div[@class='react-pdf__Page' and @data-page-number='2']"
  1493 | ).click();
  1494 |
  1495 |      await expect(page.locator('#renderList')).toContainText('2 of 4');
  1496 |      const page3 = page.locator(
  1497 |   "//div[contains(@class,'react-pdf__Document')]//div[@class='react-pdf__Page' and @data-page-number='3']"
  1498 | );
  1499 |
  1500 | await page3.scrollIntoViewIfNeeded();
  1501 | await page3.first().click();
  1502 |    await expect(page.locator('#renderList')).toContainText('3 of 4');
  1503 | const page4 = page.locator(
  1504 |   "//div[contains(@class,'react-pdf__Document')]//div[@class='react-pdf__Page' and @data-page-number='4']"
  1505 | );
  1506 | await page4.first().click();
  1507 |      await expect(page.locator('#renderList')).toContainText('4 of 4');
  1508 | await commonSteps.waitAndPlaceSignatureAndClickSave('signature', 600, 200);
  1509 | await commonSteps.dragAndDrop('stamp', 600, 350);
  1510 | await commonSteps.uploadStamp();
  1511 | await commonSteps.ClickSavebuttonSignerModal();
  1512 | await commonSteps.dragAndDrop('initials', 600, 400);
  1513 | await commonSteps.ClickSavebuttonSignerModal();
  1514 | await commonSteps.dragAndDrop('name', 600,300);
  1515 | await commonSteps.fillTextField('please enter text', 'Pravin Testing account');
  1516 | await commonSteps.ClickSavebuttonSignerModal();
  1517 | await commonSteps.dragAndDrop('job title', 600, 400);
  1518 | await commonSteps.fillTextField('please enter text', 'Quality analystAA');
  1519 | await commonSteps.ClickSavebuttonSignerModal();
  1520 | await commonSteps.dragAndDrop('company', 600, 430);
  1521 | await commonSteps.fillTextField('please enter text', 'OpenSign pvt ltd');
  1522 | await commonSteps.ClickSavebuttonSignerModal();
  1523 | await commonSteps.dragAndDrop('text', 600, 500);
  1524 | await commonSteps.fillTextField('text', '20 wood street sanfransisco');
  1525 | await commonSteps.ClickSavebuttonSignerModal();
  1526 | await commonSteps.dragAndDrop('date', 600, 520);
  1527 | const today = new Date();
  1528 | // Format the date as MM/DD/YYYY
  1529 | const formattedDate = `${(today.getMonth() + 1).toString().padStart(2, '0')}/` +
  1530 |                       `${today.getDate().toString().padStart(2, '0')}/` + 
  1531 |                       `${today.getFullYear()}`;
  1532 | console.log('Today\'s date:', formattedDate);  // Extract day number as text
  1533 | //await commonSteps.clickDateFieldOnTheSignerPad(formattedDate);
  1534 | await commonSteps.clickDateFieldOnTheSignerPad_Without_date();
  1535 | // Function to get the day with the appropriate suffix
  1536 | function getDayWithSuffix(day) {
  1537 |   if (day >= 11 && day <= 13) return `${day}th`;
  1538 |   switch (day % 10) {
  1539 |     case 1: return `${day}st`;
  1540 |
  1541 |     case 2: return `${day}nd`;  
  1542 |     case 3: return `${day}rd`;
  1543 |     default: return `${day}th`;
  1544 |   }   
  1545 | }
  1546 | // Calculate the target date (today + 2 days)
  1547 | today.setDate(today.getDate() + 2);
  1548 | const dayOfWeek = today.toLocaleString('default', { weekday: 'long' }); // e.g., "Friday" 
  1549 | const month = today.toLocaleString('default', { month: 'long' });       // e.g., "May"
  1550 | const day = today.getDate();                                            // e.g., 2
  1551 | const year = today.getFullYear();                                       // e.g., 2025
  1552 | const dayWithSuffix = getDayWithSuffix(day);
  1553 | const ariaLabelValue = `Choose ${dayOfWeek}, ${month} ${dayWithSuffix}, ${year}`;
  1554 | await commonSteps.selectCalendarDateByLabel(ariaLabelValue);
  1555 | await commonSteps.ClickSavebuttonSignerModal();
  1556 | await commonSteps.dragAndDrop('checkbox', 600, 530);
  1557 | await commonSteps.ClickSavebuttonSignerModal();
```